const Button = () => {
  return (
    <button
      type="submit"
      className="bg-orange-700 text-Neutral-900 
      x-full rounded-xl py-4 text-xl font-bold cursor-pointer hover:bg-orange-500 z-100"
    >
      Generate my ticket
    </button>
  );
};

export default Button;
